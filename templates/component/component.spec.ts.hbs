import { mount } from "@vue/test-utils";

describe("<{{pascalCase name}}/> component", async () => {
  const component = mount({{pascalCase name}}, {
    props: {},
  });

  it("…", () => {});

  component.unmount();
});
